---
description: 
globs: 
alwaysApply: true
---
<DRY_NH_SWITCH>
- All NixOS build/switch operations MUST use `nh os switch --dry`.
- NEVER perform a build or switch without the `--dry` flag.
- This rule is intended to maximize safety, prevent unintended changes, and ensure reproducibility.
</DRY_NH_SWITCH>
