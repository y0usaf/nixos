_: {
  system.defaults.CustomUserPreferences."com.apple.screencapture" = {
    "location" = "~/Pictures/Screenshots"; # Default location but won't save (clipboard mode)
    "type" = "png"; # PNG format
    "disable-shadow" = 1; # No shadow
    "clipboard" = true; # Copy to clipboard instead of file
  };
}
