{
  config,
  lib,
  pkgs,
  ...
}: let
  inherit (lib) attrByPath mkEnableOption mkIf mkOption optionals optionalAttrs types;
  inherit (types) anything attrsOf bool str;
  inherit (config) user;

  claudeCodeSkillEnabled = skillName:
    attrByPath ["user" "dev" "claude-code" "skills" skillName "enable"] true config;
in {
  options.user.dev.claude-code = {
    enable = mkEnableOption "Claude Code development tools";

    model = mkOption {
      type = str;
      default = "opus";
      description = "Claude model to use";
    };

    subagentModel = mkOption {
      type = str;
      default = "opus";
      description = "Claude model to use for subagents";
    };

    effortLevel = mkOption {
      type = str;
      default = "high";
      description = "Claude Code effort level to use";
    };

    memory = {
      autoMemoryEnabled = mkOption {
        type = bool;
        default = false;
        description = ''
          Whether Claude Code's auto-memory is enabled. Auto-memory persists
          facts to `~/.claude/projects/<project>/memory/` (MEMORY.md plus
          linked files) and recalls them across sessions.

          Maps to the top-level `autoMemoryEnabled` settings.json key
          (a boolean that defaults to `true` in Claude Code itself). Set to
          `false` to stop Claude from reading or writing the auto-memory
          directory.

          Note: the older `memory = { enabled = false; }` key is NOT part of
          Claude Code's settings schema and is a silent no-op — this option
          uses the real `autoMemoryEnabled` key instead. You can also force
          auto-memory off via the `CLAUDE_CODE_DISABLE_AUTO_MEMORY` env var
          (higher precedence) or, per-invocation, the `--bare`/`--safe-mode`
          CLI flags.
        '';
      };

      autoMemoryDirectory = mkOption {
        type = types.nullOr str;
        default = null;
        example = "~/.claude/memory";
        description = ''
          Custom directory for auto-memory storage instead of the default
          `~/.claude/projects/<project>/memory/`. Maps to the
          `autoMemoryDirectory` settings.json key; must be an absolute path
          or start with `~/`. Leave `null` to use the default per-project
          location. This relocates memory, it does not disable it.
        '';
      };

      disableClaudeMdFiles = mkOption {
        type = bool;
        default = false;
        description = ''
          Prevent loading CLAUDE.md and `.claude/rules` memory files into
          context (user, project, local, and managed). Sets the
          `CLAUDE_CODE_DISABLE_CLAUDE_MDS` env var.

          This is the *other* Claude Code memory system (CLAUDE.md
          instruction files), separate from auto-memory (MEMORY.md); it does
          not by itself disable auto-memory — use `autoMemoryEnabled` for
          that. Warning: enabling this also suppresses the global
          `~/.claude/CLAUDE.md` rules generated by this module.
        '';
      };
    };

    apiGateway = {
      baseUrl = mkOption {
        type = str;
        default = "";
        description = ''
          Custom API base URL, e.g. a Vercel AI Gateway endpoint.
          Sets ANTHROPIC_BASE_URL in settings.json.
          Leave empty to use Anthropic's API directly.
        '';
      };

      apiKeyFile = mkOption {
        type = types.nullOr str;
        default = null;
        example = "/home/y0usaf/Tokens/AI_GATEWAY_API_KEY.txt";
        description = ''
          Path (as a string, not a path literal) to a file containing the
          gateway API key/token. Appends a nushell env.nu snippet that reads
          ANTHROPIC_API_KEY from this file at shell startup.

          Must be a string so the path is read at runtime by nushell — a path
          literal would copy the file into the world-readable Nix store at
          eval time, leaking the secret.
        '';
      };

      models = mkOption {
        type = attrsOf str;
        default = {};
        example = {
          ANTHROPIC_DEFAULT_OPUS_MODEL = "glm-5.2-fast[1m]";
          ANTHROPIC_DEFAULT_SONNET_MODEL = "glm-5.2-fast[1m]";
          ANTHROPIC_DEFAULT_HAIKU_MODEL = "glm-5.2-fast[1m]";
        };
        description = ''
          Maps Claude Code's model tiers to gateway model IDs via env vars.
          When using a gateway, set these instead of `model`/`subagentModel`,
          which only select the logical tier (opus/sonnet/haiku).
        '';
      };

      extraEnv = mkOption {
        type = attrsOf str;
        default = {};
        example = {CLAUDE_CODE_AUTO_COMPACT_WINDOW = "1000000";};
        description = "Extra env vars merged into settings.json (e.g. auto-compact window).";
      };
    };

    providers."vercel-ai-gateway" = {
      enable = mkOption {
        type = bool;
        default = false;
        description = "Enable the Vercel AI Gateway provider for Claude Code.";
      };

      baseUrl = mkOption {
        type = str;
        default = "https://ai-gateway.vercel.sh";
        description = ''
          Vercel AI Gateway base URL for Claude-compatible requests.
          Sets ANTHROPIC_BASE_URL in settings.json when enabled.
        '';
      };

      apiKeyFile = mkOption {
        type = types.nullOr str;
        default = null;
        example = "/home/y0usaf/Tokens/AI_GATEWAY_API_KEY.txt";
        description = ''
          Path (as a string, not a path literal) to a file containing the
          Vercel AI Gateway API key/token. Appends a nushell env.nu snippet
          that reads ANTHROPIC_AUTH_TOKEN from this file at shell startup.
        '';
      };

      models = mkOption {
        type = attrsOf str;
        default = {};
        example = {
          ANTHROPIC_DEFAULT_OPUS_MODEL = "zai/glm-5.2";
          ANTHROPIC_DEFAULT_SONNET_MODEL = "zai/glm-5.2";
          ANTHROPIC_DEFAULT_HAIKU_MODEL = "zai/glm-4.7-flash";
        };
        description = ''
          Model routing env vars merged into Claude Code settings.json when
          the Vercel AI Gateway provider is enabled.
        '';
      };

      extraEnv = mkOption {
        type = attrsOf str;
        default = {};
        example = {CLAUDE_CODE_AUTO_COMPACT_WINDOW = "1000000";};
        description = "Extra env vars merged into settings.json for the Vercel AI Gateway provider.";
      };
    };

    plugins = mkOption {
      type = attrsOf anything;
      default = {};
      description = "Local Claude Code marketplace plugin definitions.";
    };

    enabledPlugins = mkOption {
      type = attrsOf bool;
      default =
        {
          "audio-notify@y0usaf-marketplace" = true;
          "instructify@y0usaf-marketplace" = true;
          "purr@ai-eng-plugins" = true;
          "skillify@ai-eng-plugins" = true;
          "skills-framework@ai-eng-plugins" = true;
          "the-chopper@ai-eng-plugins" = true;
        }
        // optionalAttrs ((attrByPath ["user" "tools" "gh" "enable"] false config) && claudeCodeSkillEnabled "gh") {
          "gh@y0usaf-marketplace" = true;
        }
        // optionalAttrs (claudeCodeSkillEnabled "linear-cli") {
          "linear-cli@y0usaf-marketplace" = true;
        }
        // optionalAttrs ((attrByPath ["user" "dev" "work" "agent-slack" "enable"] false config) && claudeCodeSkillEnabled "agent-slack") {
          "agent-slack@y0usaf-marketplace" = true;
        };
      description = ''
        Claude Code plugins keyed by `plugin@marketplace`.
        False values are omitted from the generated `settings.json`.
      '';
    };

    extraKnownMarketplaces = mkOption {
      type = attrsOf (attrsOf anything);
      default = {
        "y0usaf-marketplace" = {
          source = {
            source = "directory";
            path = "${config.user.homeDirectory}/.config/claude";
          };
        };
        "ai-eng-plugins" = {
          source = {
            source = "git";
            owner = "Cook-Unity";
            repo = "ai-eng-plugins";
            url = "git@github.com:Cook-Unity/ai-eng-plugins.git";
          };
        };
        "claude-code-plugins" = {
          source = {
            source = "github";
            repo = "anthropics/claude-code";
          };
        };
      };
      description = "Additional Claude Code plugin marketplaces.";
    };

    skills = mkOption {
      type = attrsOf (types.submodule {
        options.enable = mkOption {
          type = bool;
          default = true;
          description = "Whether to enable this Claude Code skill plugin.";
        };
      });
      default = {};
      description = "Per-skill Claude Code enable switches.";
    };
  };

  config = mkIf user.dev.claude-code.enable {
    assertions = let
      gatewayCfg = user.dev.claude-code.providers."vercel-ai-gateway";
      legacyCfg = user.dev.claude-code.apiGateway;
      legacyConfigured =
        legacyCfg.baseUrl
        != ""
        || legacyCfg.apiKeyFile != null
        || legacyCfg.models != {}
        || legacyCfg.extraEnv != {};
    in [
      {
        assertion = !(gatewayCfg.enable && legacyConfigured);
        message = ''
          user.dev.claude-code.apiGateway and
          user.dev.claude-code.providers."vercel-ai-gateway" are both configured.
          Use only one provider configuration path.
        '';
      }
    ];

    environment.systemPackages =
      optionals (!config.programs.tweakcc.enable) [pkgs.claude-code]
      ++ [
        (pkgs.writeShellScriptBin "bunclaude" ''
          exec ${pkgs.bun}/bin/bunx --bun @anthropic-ai/claude-code --allow-dangerously-skip-permissions "$@"
        '')
      ];

    programs.tweakcc = {
      enable = false;
      settings = {
        misc = {
          hideStartupBanner = true;
          expandThinkingBlocks = false;
        };
        claudeMdAltNames = ["AGENTS.md"];
      };
    };
  };
}
